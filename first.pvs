first : THEORY
BEGIN
  
  Cint  : TYPE = below(1000)
  Coord : TYPE = [below(2) -> Cint]
  
  f(A:Coord) : Coord =
    let B = A in
      if B(0) = 0 then B with [(0) := 0]
      else B with  
  
  g(A:Arr, B:Arr):Cint = A(0)
  
  h(a:Cint):Arr = lambda(x:Cint): a
  
  x:int = g( h(0), h(1) )
  
END first
