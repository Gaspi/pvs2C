draft : THEORY
BEGIN
  
  list : TYPE = [# car:int, cdr:list #]
  
  rec : TYPE = [# n : int,
      	       	  arr : [below(n) -> int],
		   #]
  
  f(t: rec ) : rec = t with [ `n := 0 ]
  
END draft

